# 🔧 禁用 Supabase 邮箱验证

## 问题

注册后无法登录，显示错误：
```
Email not confirmed
```

## 原因

Supabase 默认启用了邮箱验证功能，新用户注册后需要点击验证邮件中的链接才能登录。

---

## ✅ 解决方案：禁用邮箱验证

### 步骤 1：访问 Supabase Dashboard

```
https://supabase.com/dashboard
```

### 步骤 2：选择项目

选择项目：`vcoioqystzyztgrgesjw`

### 步骤 3：进入认证设置

1. 点击左侧菜单 **Authentication**
2. 点击 **Settings**（或 **Providers**）

### 步骤 4：禁用邮箱验证

找到以下设置：

**Email Auth** 部分：
- 找到 **"Enable email confirmations"** 或 **"Confirm email"**
- **关闭**这个开关（设置为 OFF/禁用）

或者在 **Email** 标签页中：
- 找到 **"Confirm email"**
- 取消勾选

### 步骤 5：保存设置

点击 **Save** 按钮保存更改

---

## 🧪 测试

### 方法 1：重新注册

禁用邮箱验证后：
1. 使用新邮箱注册（如 `test2@example.com`）
2. 注册后应该可以直接登录

### 方法 2：手动确认现有用户

如果想使用已注册的邮箱：

1. 在 Supabase Dashboard
2. 点击 **Authentication** → **Users**
3. 找到您的用户（`sevenoy@gmail.com`）
4. 点击用户进入详情
5. 找到 **Email Confirmed** 字段
6. 手动设置为 **true** 或点击 **Confirm email** 按钮

---

## 📋 详细步骤（带截图说明）

### 在 Supabase Dashboard 中：

#### 选项 A：禁用邮箱验证（推荐）

```
Authentication → Settings → Email Auth
找到：Enable email confirmations
操作：关闭开关
保存：点击 Save
```

#### 选项 B：手动确认用户

```
Authentication → Users
找到：您的邮箱
点击：用户行
操作：Confirm email 或设置 email_confirmed = true
```

---

## 🔄 SQL 方式（高级）

如果在 Dashboard 中找不到设置，可以使用 SQL：

### 方法 1：手动确认特定用户

在 SQL Editor 执行：

```sql
-- 确认特定用户的邮箱
UPDATE auth.users 
SET email_confirmed_at = NOW(),
    confirmed_at = NOW()
WHERE email = 'sevenoy@gmail.com';
```

### 方法 2：确认所有用户

```sql
-- 确认所有未验证的用户
UPDATE auth.users 
SET email_confirmed_at = NOW(),
    confirmed_at = NOW()
WHERE email_confirmed_at IS NULL;
```

---

## ✅ 验证修复

### 测试登录

1. 访问：https://sevenoy.github.io/meds/
2. 使用您的邮箱和密码登录
3. 应该可以成功登录

---

## 💡 建议

### 开发/测试环境

**推荐禁用邮箱验证**，因为：
- ✅ 方便测试
- ✅ 无需配置邮件服务
- ✅ 快速注册和登录

### 生产环境

**考虑启用邮箱验证**，因为：
- ✅ 验证真实邮箱
- ✅ 防止垃圾注册
- ✅ 提高安全性

但需要配置邮件服务（SMTP）。

---

## 🚨 常见问题

### 问题 1：找不到邮箱验证设置

**位置可能在**：
- Authentication → Settings → Email
- Authentication → Providers → Email
- Authentication → Email Templates

### 问题 2：关闭后仍然要求验证

**解决方案**：
1. 确认设置已保存
2. 清除浏览器缓存
3. 使用新邮箱重新注册测试

### 问题 3：想要启用邮箱验证

**需要配置**：
1. SMTP 服务器设置
2. 邮件模板
3. 验证链接重定向

---

## 📚 相关文档

Supabase 官方文档：
- [Email Authentication](https://supabase.com/docs/guides/auth/auth-email)
- [Auth Settings](https://supabase.com/docs/guides/auth/auth-email#configure-email-settings)

---

## ✅ 快速操作清单

```
[ ] 1. 访问 https://supabase.com/dashboard
[ ] 2. 选择项目 vcoioqystzyztgrgesjw
[ ] 3. Authentication → Settings
[ ] 4. 找到 "Enable email confirmations"
[ ] 5. 关闭开关
[ ] 6. 点击 Save
[ ] 7. 测试登录
```

---

**完成后就可以直接登录了！** 🎉

