# 📝 本地测试指南 - V260105.10

## 🚨 GitHub Pages 更新太慢的解决方案

由于 GitHub Pages 的缓存和部署延迟,我们使用本地测试来验证最新代码。

---

## ✅ 方法1: 使用已构建的 dist 文件 (推荐)

### 步骤:

1. **打开终端**

2. **启动一个简单的 HTTP 服务器:**

```bash
cd /Users/lorenmac/Downloads/26年软件项目/Meds/meds/dist
python3 -m http.server 8080
```

或者:

```bash
cd /Users/lorenmac/Downloads/26年软件项目/Meds/meds/dist
npx serve -s .
```

3. **打开浏览器,访问:**

```
http://localhost:8080/
```

4. **应该看到 V260105.10!**

---

## ✅ 方法2: 直接打开 HTML 文件

**注意:** 这个方法可能有跨域问题,但可以看到版本号。

1. **在 Finder 中打开:**
   ```
   /Users/lorenmac/Downloads/26年软件项目/Meds/meds/dist/index.html
   ```

2. **右键 → 打开方式 → 浏览器**

3. **检查版本号**

---

## ✅ 方法3: 修复开发服务器权限问题

### .env 权限问题:

在终端执行:

```bash
cd /Users/lorenmac/Downloads/26年软件项目/Meds/meds
chmod 644 .env
npm run dev
```

然后访问终端显示的地址 (通常是 `http://localhost:5173`)

---

## 🔍 检查 GitHub Actions 部署状态

1. **访问:**
   ```
   https://github.com/sevenoy/meds/actions
   ```

2. **查看最新的 workflow run:**
   - 点击最上面的一个
   - 查看 "Deploy to GitHub Pages" 状态
   - 应该显示绿色 ✅

3. **如果失败了 (红色 ❌):**
   - 点击查看错误日志
   - 可能需要重新配置 GitHub Pages

---

## 🎯 快速测试命令

在终端执行:

```bash
# 方法1: 使用 Python (macOS 自带)
cd /Users/lorenmac/Downloads/26年软件项目/Meds/meds/dist && python3 -m http.server 8080
```

打开浏览器访问: `http://localhost:8080/`

---

## 📊 预期结果

**应该看到:**

```
📦 当前版本: V260105.10
✅ Supabase 登录成功: sevenoy@gmail.com
📋 已加载 0 个药物: []
```

**不应该看到:**

```
❌ V260103.01
❌ device_id 错误
❌ 系统未初始化
```

---

## 🚀 立即测试!

**复制以下命令到终端:**

```bash
cd /Users/lorenmac/Downloads/26年软件项目/Meds/meds/dist && python3 -m http.server 8080
```

**然后在浏览器打开:**

```
http://localhost:8080/
```

**告诉我结果!** 🎉
