import { logger } from '../utils/logger';
import { logger } from '../utils/logger';
import { logger } from '../utils/logger';
import { logger } from '../utils/logger';
/**
 * 应用版本号配置
 * 仅用于 logger.log、页面展示、调试信息
 * 严禁用于 URL
 */
export const APP_VERSION = 'V260107.03';
