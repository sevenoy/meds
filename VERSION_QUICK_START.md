# 🚀 快速更新版本号

## 一键更新

```bash
npm run update-version
```

就这么简单!

---

## 📖 操作步骤

1. **运行命令**
   ```bash
   npm run update-version
   ```

2. **确认更新**
   ```
   📌 当前版本: V260105.02
   ✨ 新版本号: V260105.03
   
   是否更新版本号? (y/n): y  ← 输入 y
   ```

3. **提交到Git** (可选)
   ```
   是否提交到 Git? (y/n): y  ← 输入 y
   ```

4. **推送到远程** (可选)
   ```
   是否推送到远程仓库? (y/n): y  ← 输入 y
   ```

5. **完成!**
   ```
   🎉 版本更新完成!
   📌 新版本: V260105.03
   ```

---

## 🎯 版本号规则

```
V260105.01  → 2026年1月5日 第1个版本
V260105.02  → 2026年1月5日 第2个版本 (同一天自动+1)
V260105.03  → 2026年1月5日 第3个版本
V260106.01  → 2026年1月6日 第1个版本 (新的一天重置为01)
```

**自动递增**: 同一天多次更新,序号自动递增!

---

## 💡 使用场景

### 场景1: 每次修改代码后

```bash
# 1. 修改代码
vim App.tsx

# 2. 更新版本号
npm run update-version
# 选择: y → y → y (更新 + 提交 + 推送)

# 3. 构建
npm run build
```

### 场景2: 仅更新版本号(不提交)

```bash
npm run update-version
# 选择: y → n (仅更新,不提交Git)
```

### 场景3: 批量修改后一次更新

```bash
# 修改多个文件
vim App.tsx
vim styles.css
vim README.md

# 统一更新版本号
npm run update-version
# 选择: y → y → y

# 一次性提交所有修改
git add .
git commit -m "多项功能更新 - V260105.03"
git push
```

---

## 📝 版本号显示位置

更新后,以下位置会自动显示新版本号:

1. **页面标题**: `药盒助手 V260105.03`
2. **个人中心**: `版本 V260105.03`
3. **关于页面**: `版本 V260105.03`

---

## ⚡ 其他命令

```bash
# 开发模式
npm run dev

# 构建生产版本
npm run build

# 预览构建结果
npm run preview

# 更新版本号
npm run update-version
```

---

## 🔧 故障排查

### 问题: 脚本无法运行

```bash
# 解决方案: 给脚本添加执行权限
chmod +x update-version.mjs
chmod +x update-version.sh

# 然后重试
npm run update-version
```

### 问题: 版本号没有变化

```bash
# 检查是否有未提交的修改
git status

# 如果有修改,先提交或撤销
git add .
git commit -m "保存当前修改"

# 然后重新运行
npm run update-version
```

---

**就是这么简单! 一个命令,自动更新版本号! 🎉**

详细文档请查看: `VERSION_UPDATE_GUIDE.md`

