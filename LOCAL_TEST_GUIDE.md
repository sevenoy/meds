# 前端 UI 本地测试指南

## 🚀 快速启动

### 1. 进入前端目录
```bash
cd frontend
```

### 2. 创建环境变量文件（如果不存在）
```bash
cat > .env << 'EOF'
# 本地测试模式配置
VITE_LOCAL_TEST_MODE=true
VITE_SKIP_LOGIN=true

# Supabase 配置（可选）
VITE_SUPABASE_URL=https://fzixpacqanjygrxsrcsy.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImZ6aXhwYWNxYW5qeWdyeHNyY3N5Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3Njc0Mzg2MzgsImV4cCI6MjA4MzAxNDYzOH0.6-LthX8jXaS3ZqdGbZcCe1NZ43upWckZPwAKnTKD9AU
EOF
```

### 3. 安装依赖（如果还没安装）
```bash
npm install --legacy-peer-deps
```

### 4. 启动开发服务器
```bash
npm run dev
```

### 5. 访问应用
打开浏览器访问：**http://localhost:3000**

## ✅ 测试功能清单

### 已实现的功能（可直接测试）

1. **跳过登录**
   - ✅ 已硬编码 `SKIP_LOGIN = true`，直接进入主页面
   - ✅ 无需登录即可测试所有功能

2. **药品管理**
   - ✅ 添加药品（点击"添加药品"按钮）
   - ✅ 编辑药品（点击药品卡片的编辑按钮）
   - ✅ 删除药品（点击药品卡片的删除按钮）
   - ✅ 查看药品列表

3. **拍照记录**
   - ✅ 点击药品卡片的"拍照记录"按钮
   - ✅ 调用摄像头拍照
   - ✅ 保存服药记录

4. **今日进度**
   - ✅ 显示环形进度图
   - ✅ 显示完成数量统计

5. **历史记录**
   - ✅ 显示最近7天的服药记录
   - ✅ 显示照片缩略图
   - ✅ 显示状态标识

6. **同步状态**
   - ✅ 显示同步状态指示器

## 📝 测试步骤

### 测试1: 添加药品
1. 打开 http://localhost:3000
2. 点击"添加药品"按钮
3. 填写表单：
   - 药品名称：例如"阿司匹林"
   - 剂量：例如"1片"
   - 服药时间：选择时间（如 09:00）
   - 颜色主题：选择一个颜色
4. 点击"添加"按钮
5. ✅ 验证：药品卡片出现在列表中

### 测试2: 拍照记录
1. 点击药品卡片的"拍照记录"按钮
2. 允许浏览器访问摄像头
3. 点击"拍照"按钮
4. 查看照片预览
5. 点击"确认"按钮
6. ✅ 验证：
   - 显示成功提示
   - 今日进度更新
   - 历史记录中出现新记录

### 测试3: 编辑药品
1. 点击药品卡片的编辑按钮（铅笔图标）
2. 修改药品信息
3. 点击"更新"按钮
4. ✅ 验证：药品信息已更新

### 测试4: 删除药品
1. 点击药品卡片的删除按钮（垃圾桶图标）
2. 确认删除
3. ✅ 验证：药品从列表中消失

### 测试5: 查看进度和历史
1. 查看"今日进度"卡片
   - ✅ 验证：显示完成百分比
2. 查看"最近7天记录"卡片
   - ✅ 验证：显示历史记录列表

## 🔧 本地存储

所有数据存储在浏览器的 **IndexedDB** 中：
- 数据库名称：`MedicationTrackerDB`
- 表：`medications` 和 `medicationLogs`
- 数据在浏览器关闭后仍然保留

### 清除测试数据
在浏览器控制台执行：
```javascript
// 打开浏览器开发者工具（F12）
// 在 Console 中执行：
indexedDB.deleteDatabase('MedicationTrackerDB');
// 然后刷新页面
```

## ⚠️ 注意事项

1. **摄像头权限**
   - 首次使用拍照功能时，浏览器会请求摄像头权限
   - 请点击"允许"以测试拍照功能

2. **本地测试模式**
   - 当前已启用本地测试模式，跳过登录
   - 数据仅存储在本地，不会同步到云端

3. **浏览器兼容性**
   - 推荐使用 Chrome、Edge 或 Safari
   - 需要支持 IndexedDB 和摄像头 API

4. **端口占用**
   - 默认端口：3000
   - 如果端口被占用，Vite 会自动使用下一个可用端口

## 🐛 常见问题

### 问题1: 页面空白
**解决方案**：
- 检查浏览器控制台是否有错误
- 确认依赖已正确安装：`npm install --legacy-peer-deps`
- 清除浏览器缓存并刷新

### 问题2: 摄像头无法访问
**解决方案**：
- 检查浏览器权限设置
- 确保使用 HTTPS 或 localhost（Vite 默认支持）
- 尝试刷新页面

### 问题3: 数据不显示
**解决方案**：
- 检查 IndexedDB 是否正常工作
- 在控制台执行：`localStorage.clear()` 然后刷新
- 重新添加测试数据

## 📊 测试数据示例

可以添加以下测试药品：
1. **阿司匹林** - 1片 - 09:00 - 青柠色
2. **维生素C** - 1片 - 12:00 - 蓝色
3. **钙片** - 2片 - 18:00 - 橙色

然后为每个药品拍照记录，测试完整流程。

---

**测试完成后，记得清除测试数据或使用无痕模式测试！**




