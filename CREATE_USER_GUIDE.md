# 👤 创建 Supabase 用户指南

## ✅ API Key 已验证正确

你的配置没有问题，现在需要在 Supabase 中创建用户账号。

---

## 🎯 创建用户步骤

### **方法1: 通过 Supabase Dashboard（推荐）**

#### **步骤1: 打开用户管理页面**

访问：
```
https://supabase.com/dashboard/project/ptmgncjechjprxtndqon/auth/users
```

或者：
1. 登录 https://supabase.com/dashboard
2. 选择项目：`ptmgncjechjprxtndqon`
3. 左侧菜单：**Authentication** 🔐
4. 点击：**Users**

---

#### **步骤2: 创建新用户**

点击右上角的 **Add user** 或 **Invite user** 按钮

---

#### **步骤3: 填写用户信息**

在弹出的表单中填写：

```
┌─────────────────────────────────────┐
│ Create new user                     │
├─────────────────────────────────────┤
│                                     │
│ Email *                             │
│ sevenoy@gmail.com                   │ ← 输入这个
│                                     │
│ Password *                          │
│ jiajia                              │ ← 输入这个
│                                     │
│ Confirm password *                  │
│ jiajia                              │ ← 再输入一次
│                                     │
│ □ Auto Confirm User                 │
│   ☑ 勾选这个（跳过邮箱验证）         │
│                                     │
│ [Cancel]  [Create user]             │
└─────────────────────────────────────┘
```

**重要：**
- Email: `sevenoy@gmail.com`
- Password: `jiajia`
- ✅ 勾选 **Auto Confirm User**（这样不需要邮箱验证）

---

#### **步骤4: 确认创建**

点击 **Create user** 按钮

你应该看到新用户出现在列表中：
```
sevenoy@gmail.com    Confirmed    Just now
```

---

### **方法2: 通过 SQL（高级用户）**

如果你熟悉SQL，可以在 **SQL Editor** 中执行：

```sql
-- 注意：这个方法需要手动加密密码，不推荐
-- 建议使用方法1（Dashboard）
```

---

## 🔍 验证用户是否创建成功

### **在 Dashboard 中检查**

1. 进入 **Authentication** → **Users**
2. 应该看到：
   ```
   Email: sevenoy@gmail.com
   Status: Confirmed ✅
   ```

### **通过应用测试**

1. 打开应用：`http://localhost:4173/meds/`
2. 按 F12 打开控制台
3. 尝试登录：
   - 用户名：`sevenoy`
   - 密码：`jiajia`

应该看到日志：
```
🔐 尝试登录: sevenoy@gmail.com
🌐 Supabase模式：调用登录API
📡 Supabase登录响应: {data: {...}, error: null}
✅ 登录成功
```

---

## 🆘 常见问题

### **Q1: 找不到 "Add user" 按钮？**

**A:** 检查是否在正确的位置：
- 左侧菜单：**Authentication** 🔐
- 顶部标签：**Users**
- 右上角应该有 **Add user** 或 **Invite user** 按钮

---

### **Q2: 提示 "Email already exists"？**

**A:** 用户已经存在！检查：
1. 列表中是否有 `sevenoy@gmail.com`
2. 如果有，检查状态是否为 **Confirmed**
3. 如果状态是 **Unconfirmed**，点击用户 → **Confirm email**

或者：
1. 删除旧用户
2. 重新创建

---

### **Q3: 创建后还是登录失败？**

**可能原因：**

**1. 密码不对**
- 确认 Supabase 中设置的密码是 `jiajia`
- 不是 `Jiajia` 或其他变体

**2. 用户未确认**
- 检查用户状态是否为 **Confirmed**
- 如果不是，点击用户 → **Confirm email**

**3. 缓存问题**
- 清除浏览器缓存
- 重启开发服务器

**调试步骤：**
```javascript
// 浏览器控制台执行
localStorage.clear()
location.reload(true)
// 然后重新登录
```

---

### **Q4: 登录时报 "Invalid credentials"？**

**检查清单：**
- [ ] 用户邮箱：`sevenoy@gmail.com` ✅
- [ ] 用户密码：`jiajia` ✅
- [ ] 用户状态：Confirmed ✅
- [ ] 输入用户名：`sevenoy`（不是邮箱）
- [ ] 输入密码：`jiajia`（区分大小写）

**测试直接使用邮箱登录：**
修改代码临时测试：
```javascript
// 在 LoginPage.tsx 中
const email = username.includes('@') 
  ? username 
  : `${username}@gmail.com`;
```

---

## 📊 创建用户检查清单

完成以下步骤：

- [ ] 访问 Supabase Dashboard 用户管理页面
- [ ] 点击 "Add user" 按钮
- [ ] 填写邮箱：`sevenoy@gmail.com`
- [ ] 填写密码：`jiajia`
- [ ] 勾选 "Auto Confirm User"
- [ ] 点击 "Create user"
- [ ] 确认用户出现在列表中
- [ ] 确认状态为 "Confirmed"
- [ ] 重启开发服务器
- [ ] 清除浏览器缓存
- [ ] 尝试登录

---

## 🎯 完成后的测试流程

1. **打开应用**
   ```
   http://localhost:4173/meds/
   ```

2. **打开控制台**
   按 F12

3. **清除缓存**
   ```javascript
   localStorage.clear()
   location.reload()
   ```

4. **输入登录信息**
   - 用户名：`sevenoy`
   - 密码：`jiajia`

5. **观察日志**
   应该看到：
   ```
   ✅ 登录成功
   ```

6. **进入应用**
   应该看到主界面，不再是登录页面

---

## 📞 仍然有问题？

提供以下信息：

1. **Supabase Users 列表截图**
   - 显示 `sevenoy@gmail.com` 和状态

2. **浏览器控制台完整日志**
   - 登录尝试的所有输出

3. **错误信息**
   - 页面上显示的红色提示

---

**最后更新**: 2025年12月19日
