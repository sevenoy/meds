# 本地测试账号使用指南

## 🎯 启用本地测试模式

本地测试模式允许您在不连接 Supabase 的情况下测试应用。

### 步骤 1: 启用本地测试模式

编辑 `frontend/.env` 文件，添加：

```env
VITE_LOCAL_TEST_MODE=true
```

或者直接在终端执行：

```bash
cd "/Users/lorenmac/Downloads/26年软件项目/Meds/meds/frontend"
echo "VITE_LOCAL_TEST_MODE=true" >> .env
```

### 步骤 2: 重启前端服务器

1. 停止当前运行的前端服务器（按 `Ctrl+C`）
2. 重新启动：

```bash
npm run dev
```

## 🔑 测试账号

启用本地测试模式后，可以使用**任意邮箱和密码**登录：

### 测试账号示例

- **邮箱**: `test@example.com`（或任意邮箱）
- **密码**: `123456`（或任意6位以上密码）

### 快速登录

1. 打开 http://localhost:3000
2. 输入任意邮箱（如 `test@example.com`）
3. 输入任意密码（至少6位，如 `123456`）
4. 点击"登录"或"注册"

## ⚠️ 注意事项

1. **本地测试模式仅用于开发**
   - 数据存储在浏览器的 localStorage
   - 不会同步到 Supabase
   - 刷新页面后数据可能丢失

2. **功能限制**
   - 多设备同步不可用
   - 云端存储不可用
   - 实时同步不可用

3. **关闭本地测试模式**
   - 从 `.env` 文件中移除 `VITE_LOCAL_TEST_MODE=true`
   - 或设置为 `VITE_LOCAL_TEST_MODE=false`
   - 重启前端服务器

## 🧪 测试场景

### 场景 1: 快速测试 UI
- 启用本地测试模式
- 使用任意账号登录
- 测试界面和交互

### 场景 2: 完整功能测试
- 关闭本地测试模式
- 在 Supabase 中注册真实账号
- 测试完整功能（同步、存储等）

## 📝 推荐测试账号

为了方便测试，建议使用以下账号：

```
邮箱: test@meds.local
密码: test123456
```

或者：

```
邮箱: demo@example.com
密码: demo123
```

## 🔄 切换模式

### 启用本地测试模式
```bash
cd frontend
echo "VITE_LOCAL_TEST_MODE=true" >> .env
npm run dev
```

### 关闭本地测试模式
```bash
cd frontend
# 编辑 .env 文件，移除或注释掉 VITE_LOCAL_TEST_MODE=true
npm run dev
```

